protocol bgp nixosmajordomo {
    local as OWNAS;
    ipv4 {
        next hop self;
        import none;
        export filter {
            if (net = 0.0.0.0/0) then {
                bgp_community.add((OWNAS,0));
                accept;
            }
            reject;
        };
    };
    neighbor 192.168.0.197 as 64979;
    multihop;
}
