protocol bgp nixosantifilter {
    local as OWNAS;
    ipv4 {
        next hop self;
        import filter { if (net = 0.0.0.0/0) then reject; accept; };
        export none;
    };
    neighbor 192.168.0.180 as 64993;
}
