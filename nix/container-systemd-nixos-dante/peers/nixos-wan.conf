protocol bgp nixoswan {
      local as OWNAS;
      ipv4 {
         next hop self;
         export none;
         import filter { if (net = 0.0.0.0/0) then accept; reject; };
      };
      neighbor 192.168.0.160 as 64990;
}
