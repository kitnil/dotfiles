log syslog all;

define OWNAS = 64983;

protocol device {
}

protocol kernel {
    ipv4 {
        import none;
        export filter {
            if (source = RTS_STATIC) && (net = 192.168.0.170/32) then accept;
            if (64992,0) ~ bgp_community then accept;
            reject;
        };
    };
}

protocol static {
    ipv4;
    route 192.168.0.170/32 via "eth0";
}

include "/etc/bird/peers/*.conf";

