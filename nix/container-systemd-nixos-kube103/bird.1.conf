log syslog all;

define OWNAS = 64978;

protocol device {
}

protocol kernel {
    mertic 0;
    ipv4 {
        import none;
        export all;
    };
}

protocol static {
    ipv4;
    route 192.168.0.0/24 via "eth0";
    route 0.0.0.0/0 recursive 192.168.0.1;
}
