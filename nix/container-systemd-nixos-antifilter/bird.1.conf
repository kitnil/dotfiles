log "/var/log/bird.log" all;

define OWNAS = 64993;
define OWNIP = 192.168.0.180;

protocol device {
}

protocol kernel {
    scan time 20;
    ipv4 {
        import none;
        export filter { if (net = 0.0.0.0/0) then accept; reject; };
    };
}

include "/etc/bird/peers/*.conf";
