protocol bgp nixoszapret {
    local as OWNAS;
    ipv4 {
        table downstream_routes;
        next hop self;
        export all;
        import none;
    };
    neighbor 192.168.0.175 as 64997;
}
