apiVersion: apps/v1
kind: Deployment
metadata:
  name: dante
  namespace: dante
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: dante
      app.kubernetes.io/part-of: dante
  template:
    metadata:
      labels:
        app.kubernetes.io/name: dante
        app.kubernetes.io/part-of: dante
    spec:
      hostNetwork: true
