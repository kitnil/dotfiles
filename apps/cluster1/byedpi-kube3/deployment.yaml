apiVersion: apps/v1
kind: Deployment
metadata:
  name: byedpi
spec:
  template:
    spec:
      nodeSelector:
        kubernetes.io/hostname: kube3
      tolerations:
      - key: "workstation.cluster.local"
        operator: "Equal"
        value: "true"
        effect: "NoSchedule"
