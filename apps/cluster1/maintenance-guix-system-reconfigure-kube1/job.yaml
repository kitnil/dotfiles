apiVersion: batch/v1
kind: Job
metadata:
  name: guix-system-reconfigure
spec:
  template:
    metadata:
      annotations:
        guix-default-channel-commit: 8c0fc60e
    spec:
      nodeSelector:
        kubernetes.io/hostname: kube1
