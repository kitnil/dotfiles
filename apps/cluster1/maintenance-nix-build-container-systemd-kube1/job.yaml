apiVersion: batch/v1
kind: Job
metadata:
  name: nix-container-systemd
spec:
  template:
    metadata:
      annotations:
        guix-default-channel-commit: 20720fb2
    spec:
      nodeSelector:
        kubernetes.io/hostname: kube1
