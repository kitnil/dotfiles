apiVersion: batch/v1
kind: Job
metadata:
  name: nix-container-systemd
spec:
  template:
    metadata:
      annotations:
        guix-default-channel-commit: ac44c9bc
    spec:
      nodeSelector:
        kubernetes.io/hostname: kube1
