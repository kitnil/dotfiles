apiVersion: batch/v1
kind: Job
metadata:
  name: nix-container-systemd
spec:
  template:
    metadata:
      annotations:
        guix-default-channel-commit: 7d2ef3c8
    spec:
      nodeSelector:
        kubernetes.io/hostname: kube1
