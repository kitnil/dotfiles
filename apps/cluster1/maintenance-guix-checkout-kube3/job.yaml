apiVersion: batch/v1
kind: Job
metadata:
  name: guix-checkout
spec:
  template:
    spec:
      nodeSelector:
        kubernetes.io/hostname: kube3
      tolerations:
      - key: workstation.cluster.local
        operator: Equal
        value: 'true'
        effect: NoSchedule
