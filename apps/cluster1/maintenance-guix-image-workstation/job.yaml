apiVersion: batch/v1
kind: Job
metadata:
  name: guix-image-workstation
spec:
  template:
    metadata:
      annotations:
        guix-default-channel-commit: 03992f10
    spec:
      nodeSelector:
        kubernetes.io/hostname: kube1
