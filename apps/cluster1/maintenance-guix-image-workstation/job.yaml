apiVersion: batch/v1
kind: Job
metadata:
  name: guix-image-workstation
spec:
  template:
    metadata:
      annotations:
        guix-default-channel-commit: f0e2c45b
    spec:
      nodeSelector:
        kubernetes.io/hostname: kube1
