apiVersion: batch/v1
kind: Job
metadata:
  name: guix-image-workstation
spec:
  template:
    metadata:
      annotations:
        guix-default-channel-commit: "b847589c"
    spec:
      nodeSelector:
        kubernetes.io/hostname: kube1
