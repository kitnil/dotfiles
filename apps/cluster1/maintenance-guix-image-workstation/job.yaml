apiVersion: batch/v1
kind: Job
metadata:
  name: guix-image-workstation
spec:
  template:
    metadata:
      annotations:
        guix-default-channel-commit: 767b1a1f
    spec:
      nodeSelector:
        kubernetes.io/hostname: kube1
