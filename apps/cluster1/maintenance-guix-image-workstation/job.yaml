apiVersion: batch/v1
kind: Job
metadata:
  name: guix-image-workstation
  pec:
    template:
      spec:
        nodeSelector:
          kubernetes.io/hostname: kube1
spec:
  template:
    metadata:
      annotations:
        guix-default-channel-commit: 0e482470
