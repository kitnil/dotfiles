apiVersion: batch/v1
kind: Job
metadata:
  name: git-dotfiles
spec:
  template:
    metadata:
      annotations:
        guix-default-channel-commit: c9ad072a
    spec:
      nodeSelector:
        kubernetes.io/hostname: kube3
