apiVersion: batch/v1
kind: Job
metadata:
  name: nix-home-reconfigure
spec:
  template:
    metadata:
      annotations:
        guix-default-channel-commit: 6d65dc0d
    spec:
      nodeSelector:
        kubernetes.io/hostname: kube2
