apiVersion: batch/v1
kind: Job
metadata:
  name: build-workstation-controller
spec:
  template:
    metadata:
      annotations:
        guix-default-channel-commit: a84a164a
    spec:
      nodeSelector:
        kubernetes.io/hostname: kube1
