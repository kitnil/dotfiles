apiVersion: batch/v1
kind: Job
metadata:
  name: build-workstation-controller
spec:
  template:
    metadata:
      annotations:
        guix-default-channel-commit: 4c6f8f49
    spec:
      nodeSelector:
        kubernetes.io/hostname: kube1
