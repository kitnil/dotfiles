apiVersion: batch/v1
kind: Job
metadata:
  name: guix-system-reconfigure
spec:
  template:
    metadata:
      annotations:
        guix-default-channel-commit: b46e7e1f
    spec:
      nodeSelector:
        kubernetes.io/hostname: kube3
      tolerations:
        - key: workstation.cluster.local
          operator: Equal
          value: 'true'
          effect: NoSchedule
