apiVersion: batch/v1
kind: Job
metadata:
  name: git-dotfiles
spec:
  template:
    metadata:
      annotations:
        guix-default-channel-commit: 9ac75ef9
    spec:
      nodeSelector:
        kubernetes.io/hostname: kube1
      tolerations:
        - key: edge.cluster.local/not-schedulable
          operator: Equal
          effect: NoSchedule
