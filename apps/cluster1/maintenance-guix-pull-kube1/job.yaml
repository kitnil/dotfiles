apiVersion: batch/v1
kind: Job
metadata:
  name: guix-pull
spec:
  template:
    metadata:
      annotations:
        guix-default-channel-commit: 86fa442a
    spec:
      nodeSelector:
        kubernetes.io/hostname: kube1
