apiVersion: v1
kind: Service
metadata:
  name: redsocks
  labels:
    app.kubernetes.io/name: redsocks
    app.kubernetes.io/part-of: redsocks
spec:
  ports:
  - port: 1080
    protocol: TCP
    targetPort: 1080
    name: socks5
  selector:
    app.kubernetes.io/name: redsocks
    app.kubernetes.io/part-of: redsocks
  type: ClusterIP
  clusterIP: '10.8.0.3'
