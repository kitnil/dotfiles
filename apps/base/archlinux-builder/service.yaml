apiVersion: v1
kind: Service
metadata:
  name: workstation
spec:
  ports:
  - name: scream
    port: 16400
    protocol: TCP
    targetPort: scream
  selector:
    app.kubernetes.io/name: workstation
  type: LoadBalancer
