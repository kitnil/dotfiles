apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: state-to-git
resources:
- namespace.yaml
- job.yaml
configMapGenerator:
- name: git-configuration
  files:
  - .gitconfig=rootfs/root/.gitconfig
- name: rootfs
  files:
  - passwd=rootfs/etc/passwd
