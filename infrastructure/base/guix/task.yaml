apiVersion: tekton.dev/v1beta1
kind: Task
metadata:
  name: example-task-name
spec:
  resources:
    inputs:
      - name: workspace
        type: git
  steps:
    - name: debian-example
      image: debian:11
      script: |
        #!/usr/bin/env bash
        echo "Hello from Bash"
