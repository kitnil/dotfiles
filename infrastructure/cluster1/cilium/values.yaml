k8sServiceHost: "192.168.25.10"
k8sServicePort: "6443"

cluster:
  name: "cluster1"
  id: 1

# -- Enable installation of PodCIDR routes between worker
# nodes if worker nodes share a common L2 network segment.
ipv4-native-routing-cidr: 10.0.0.0/14
