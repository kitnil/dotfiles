apiVersion: cilium.io/v2alpha1
kind: CiliumNodeConfig
metadata:
  namespace: kube-system
  name: direct-routing-device-kube1
spec:
  nodeSelector:
    matchLabels:
      kubernetes.io/hostname: kube1
  defaults:
    devices: "br0 enp34s0 tapvpn1"
    direct-routing-device: "br0,tapvpn1,enp34s0"
