apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: k8s-image-availability-exporter
resources:
- namespace.yaml
- release.yaml
configMapGenerator:
- name: k8s-image-availability-exporter-values
  files:
  - values.yaml=values.yaml
configurations:
- kustomizeconfig.yaml
