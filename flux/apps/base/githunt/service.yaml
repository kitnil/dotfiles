apiVersion: v1
kind: Service
metadata:
  name: githunt
  labels:
    app.kubernetes.io/name: githunt
    app.kubernetes.io/part-of: githunt
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 8080
    name: http
  selector:
    app.kubernetes.io/name: githunt
    app.kubernetes.io/part-of: githunt
  type: ClusterIP
