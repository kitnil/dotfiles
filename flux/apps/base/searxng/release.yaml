apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: searxng
spec:
  chart:
    spec:
      chart: searxng
      reconcileStrategy: ChartVersion
      version: 1.0.7
      sourceRef:
        kind: HelmRepository
        name: kubito
        namespace: flux-system
  interval: 1m0s
  valuesFrom:
  - kind: ConfigMap
    name: searxng-values
