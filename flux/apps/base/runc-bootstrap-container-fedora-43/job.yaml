apiVersion: batch/v1
kind: Job
metadata:
  name: runc-bootstrap-container
spec:
  template:
    spec:
      containers:
      - name: bootstrap
        env:
        - name: RUNC_BOOTSTRAP_CONTAINER_IMAGE
          value: fedora:43
