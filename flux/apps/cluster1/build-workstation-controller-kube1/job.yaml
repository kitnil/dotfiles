apiVersion: batch/v1
kind: Job
metadata:
  name: build-workstation-controller
spec:
  template:
    metadata:
      annotations:
        guix-default-channel-commit: 2a138e0b
    spec:
      nodeSelector:
        kubernetes.io/hostname: kube1
