apiVersion: v1
kind: Service
metadata:
  name: tor
  labels:
    app.kubernetes.io/name: tor
    app.kubernetes.io/part-of: tor
spec:
  ports:
  - port: 9150
    protocol: TCP
    targetPort: 9150
    name: tor
  selector:
    app.kubernetes.io/name: tor
    app.kubernetes.io/part-of: tor
  type: ClusterIP
