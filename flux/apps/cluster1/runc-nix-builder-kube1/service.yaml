apiVersion: v1
kind: Service
metadata:
  name: runc-kube1
  labels:
    app.kubernetes.io/name: runc-kube1-nix-builder
    app.kubernetes.io/part-of: nix
spec:
  ports:
  - port: 22
    protocol: TCP
    targetPort: 22
    name: ssh
  selector:
    app.kubernetes.io/name: runc-kube1-nix-builder
    app.kubernetes.io/part-of: nix
  type: ClusterIP
