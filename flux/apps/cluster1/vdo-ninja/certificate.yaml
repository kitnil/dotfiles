apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: certificate
spec:
  secretName: vdo-ninja-cert
  issuerRef:
    group: cert-manager.io
    name: letsencrypt
    kind: ClusterIssuer
  dnsNames:
  - vdo.home.wugi.info
  usages:
  - digital signature
  - key encipherment
