#!/bin/bash

setup()
{
    file="/run/opengl-driver"
    if [[ -f "$file" ]]
    then
        sudo ln -s "$(nix-build vaapiIntel)" "$file"
    else
        echo "File $file already exists."
    fi
}

nixGLIntel exec -a "$0" obs "$@"
