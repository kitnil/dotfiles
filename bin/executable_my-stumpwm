;; -*-lisp-*-

;; https://github.com/stumpwm/stumpwm/wiki/SetUp

(require :asdf)
(require :stumpwm)
(require :swank)

;; (swank-loader::setup)

(stumpwm:stumpwm)

(swank-loader:init)
(swank:create-server :dont-close t)
(sb-thread:make-thread
 (lambda ()
   (swank:create-server :dont-close t)))

;; (sb-thread:make-thread
;;  (lambda ()
;;    (stumpwm:stumpwm)))
