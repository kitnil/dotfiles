#!@EMACS@ --script

(progn
  (with-current-buffer
      (get-buffer "*Benchmark Init Results Tabulated*")
    (princ (buffer-substring-no-properties (point-min)
                                           (point-max))
           #'external-debugging-output))
  (kill-emacs))
