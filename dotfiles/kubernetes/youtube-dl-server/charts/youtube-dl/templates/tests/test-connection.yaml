apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "youtube-dl.fullname" . }}-test-connection"
  labels:
    {{- include "youtube-dl.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "youtube-dl.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
